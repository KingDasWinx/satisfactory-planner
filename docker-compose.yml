services:

  # ── PostgreSQL ────────────────────────────────────────────────
  # Descomente este bloco se quiser subir o banco localmente via Docker.
  # Lembre de ajustar DATABASE_URL no .env para:
  #   postgresql://planner:planner@db:5432/planner
  #
  # db:
  #   image: postgres:16-alpine
  #   restart: unless-stopped
  #   environment:
  #     POSTGRES_USER:     ${POSTGRES_USER:-planner}
  #     POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-planner}
  #     POSTGRES_DB:       ${POSTGRES_DB:-planner}
  #   volumes:
  #     - postgres_data:/var/lib/postgresql/data
  #   healthcheck:
  #     test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-planner} -d ${POSTGRES_DB:-planner}"]
  #     interval: 10s
  #     timeout: 5s
  #     retries: 5

  # ── Next.js app ───────────────────────────────────────────────
  app:
    build:
      context: .
      dockerfile: Dockerfile
    restart: unless-stopped
    ports:
      - "${APP_PORT:-3000}:3000"
    # Se reativar o serviço db acima, descomente as linhas abaixo:
    # depends_on:
    #   db:
    #     condition: service_healthy
    env_file:
      - .env

# volumes:
#   postgres_data:
